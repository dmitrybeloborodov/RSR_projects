oversion: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6.6
    steps:
      - checkout
      - run:
          name: initializing environment
          command: |
            python3 -m virtualenv venv
            . venv/bin/activate
            pip install -r requirements.txt
      - run:
          name: executing code and submitting
          command: |
            . venv/bin/activate
            mkdir .kaggle
            cd .kaggle
            echo "{"username":\"${KAGGLE_USERNAME}\","key":\"${KAGGLE_KEY}\}" > kaggle.json
            cd ..
            kaggle competitions download titanic
            jupyter-nbconvert --execute titanic_ml.ipynb
            osf -p t53z4 upload -f submission.csv submission.csv
            mkdir reports
            mv titanic_ml.html reports/
      - store_artifacts:
          path: reports/
          destination: reports/
